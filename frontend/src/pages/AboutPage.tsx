import { Card, CardContent, Container, Grid, Typography } from '@mui/material';
const AboutPage = () => <Container maxWidth="lg" sx={{ py: 5 }}><Typography variant="h4" component="h1">About MyHospital</Typography><Typography color="text.secondary" sx={{ mb: 3 }}>This portal presents configurable hospital information and patient services.</Typography><Grid container spacing={3}>{[['Hospital overview', 'Add the hospital’s verified overview here.'], ['Mission', 'Add the hospital’s approved mission statement here.'], ['Services and departments', 'Services, departments, and facilities are shown from hospital-managed records when configured.'], ['Facilities and contact', 'Maintain the hospital address, contact details, working hours, and emergency details in the administration settings.']].map(([title, body]) => <Grid item xs={12} md={6} key={title}><Card><CardContent><Typography variant="h6">{title}</Typography><Typography color="text.secondary" sx={{ mt: 1 }}>{body}</Typography></CardContent></Card></Grid>)}</Grid></Container>;
export default AboutPage;
